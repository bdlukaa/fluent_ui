// This file implements top display mode panel
// See https://docs.microsoft.com/en-us/windows/uwp/design/controls-and-patterns/navigationview#top

import 'package:fluent_ui/fluent_ui.dart';

/// The pane is positioned above the content.
class TopNavigationPanel extends StatelessWidget {
  const TopNavigationPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
