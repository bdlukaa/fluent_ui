import 'package:fluent_ui/fluent_ui.dart';

class AppHistoryView extends StatelessWidget {
  const AppHistoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('App History Content Goes Here'));
  }
}
